name: community_care
description: Neighbourly assistance hub for community care

version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Add any additional dependencies your project may need
  # example:
  # cupertino_icons: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

# Add any additional development dependencies if needed

flutter:
  uses-material-design: true

# Add any other Flutter-specific configuration as needed
